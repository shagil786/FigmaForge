Hooks are executed as separate processes: stdin receives a JSON tool invocation object, stdout must emit a JSON result, and exit codes encode policy decisions (0 pass/skip, 1 ask/block, 2 fatal). External mutation gating requires no setup beyond having the script executable; post-edit validation requires the corresponding language toolchain installed on PATH or checks are silently skipped.